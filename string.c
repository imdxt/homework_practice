#include <stdio.h>
#include <string.h>
int main(int argc, const char *argv[])
{
   // int i;
      char a,b,c;
   // char a[5]="abcd";
   // for(i=0;i<strlen(a);i++)
   // {
        a=getchar();
        b=getchar();
        c=getchar();
        printf("%d\n",a);
        printf("%d\n",b);
        printf("%d\n",c);
       // putchar(a[i]);
       //putchar(c);
   // }
   //putchar('\n');
    return 0;
}
